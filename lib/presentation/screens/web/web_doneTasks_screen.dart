import 'package:flutter/material.dart';

class WebDoneTasksScreen extends StatelessWidget {
  const WebDoneTasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tareas hechas'),
    );
  }
}