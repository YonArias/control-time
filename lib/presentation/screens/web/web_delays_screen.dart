import 'package:flutter/material.dart';

class WebDelaysScreen extends StatelessWidget {
  const WebDelaysScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Delays'),
    );
  }
}