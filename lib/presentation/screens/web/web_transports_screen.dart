import 'package:flutter/material.dart';

class WebTransportsScreen extends StatelessWidget {
  const WebTransportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Transportes'),
    );
  }
}